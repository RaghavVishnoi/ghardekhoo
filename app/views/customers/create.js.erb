<% if @show_errors != true %>
	window.location = '/'
<% else %>
	renderFlash('div.pop_flash_container', $("<%=j render 'layouts/flash_message'%>"));
	scrollTop()
	<% flash.discard %>
	stop_spin();
	discard_flash();
<% end %>