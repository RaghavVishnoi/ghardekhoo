<section class="customer-logos slider">
      <% urls.each do |url|%>
            <div class="slide pointer" data-toggle="modal" data-target="#retailerDetailsModal"><img src="<%= url %>" height="130"></div> 
      <% end %>    
</section>