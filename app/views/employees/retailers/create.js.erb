<% if @show_errors != true %>
	window.location = '/employees/retailers'
<% else %>
	renderFlash('div.flash_container', $("<%=j render 'layouts/flash_message'%>"));
	scrollTop()
	<% flash.discard %>
	stop_spin();
	discard_flash();
<% end %>