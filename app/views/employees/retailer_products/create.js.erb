<% if @show_errors != true %>
	window.location = '<%= @redirect_url %>'
<% else %>
	renderFlash('div.flash_container', $("<%=j render 'layouts/flash_message'%>"));
	scrollTop()
	<% flash.discard %>
	stop_spin();
<% end %>