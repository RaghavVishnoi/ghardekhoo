  
    <%= render 'commons/template/header' %> 

    <div id="search_retailer_container">
      <%= render 'commons/template/search_retailers', states: @states, cities: @cities, default_state: @default_state, default_city: @default_city %> 
    </div>  

    <div class="wrapper">
      
      <div id="category-container">
        <%= render 'commons/template/categories', categories: @categories, retailers_count: @retailers_count %>
      </div>  

      <%= render 'commons/template/featured_list' %>

      <%= render 'commons/template/features' %>

      <%= render 'commons/template/location' %>

    </div>

    <%= render 'commons/template/counter' %>

    <%= render 'commons/template/footer' %> 
    
    <!-- Go To Top Link -->
    <a href="#" class="back-to-top">
      <i class="fa fa-angle-up"></i>
    </a>

<%= render 'commons/hidden_forms' %>

<script>
  window.onload = function() {
  var startPos;
  var geoSuccess = function(position) {
    startPos = position;
    $('#get_location #lat').val(startPos.coords.latitude)
    $('#get_location #lng').val(startPos.coords.longitude)
    $('#get_location').trigger('submit.rails');
  };
  var geoError = function(error) {
    console.log('Error occurred. Error code: ' + error.code);
  };
  navigator.geolocation.getCurrentPosition(geoSuccess, geoError);
};
</script>