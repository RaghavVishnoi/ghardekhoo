<% if @error == true %>
	renderFlash('div.flash_container', $("<%=j render 'layouts/flash_message'%>"));
	scrollTop()
	<% flash.discard %>
	stop_spin();
<% else %>
	location.href = '<%= @redirect_path %>'
<% end %>